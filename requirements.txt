# Person Lock System Requirements
# Python 3.11.13 (recommended for MediaPipe compatibility)

# Core computer vision and image processing
opencv-python>=4.8.0
numpy>=1.21.0
Pillow>=8.3.0

# Object detection and tracking  
ultralytics>=8.0.0

# Gesture recognition and pose estimation
mediapipe>=0.10.0

# Person re-identification
torchreid>=1.4.0
torch>=1.12.0
torchvision>=0.13.0

# GUI framework (for direction control display)
tkinter  # Usually included with Python, but listed for completeness

# Serial communication for command streaming
pyserial>=3.5

# Utilities
urllib3>=1.26.0

# Optional: For CUDA support (if using GPU)
# torch>=1.12.0+cu116
# torchvision>=0.13.0+cu116

# Note: Install instructions
# 1. Basic CPU installation:
#    pip install -r requirements.txt
#
# 2. For GPU support, install PyTorch with CUDA first:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    pip install -r requirements.txt